# 本项目的目的
1. 对于之前有一点基础的前端，知道一点php，java项目。发现使用现代的前端工具比如nuxt+vue，有许多的不同，于是就有了我的一个超级入门的代码。代码官网中有，但是不全如果没有基础是运行不了的。
2. 如果是一个老手，对于这个点逻辑来说简单，一看就懂。但是对于新手而言，似乎有没必要理解新的，直接用nuxt3就可以了。从这个角度来看，这个项目有点尴尬。
3. 其实就是自己快忘记的时候可以查看下代码到底什么用


# 可以了解到什么
- 模板的作用，app.vue这个文件的作用。nuxt4的布局。
- 文件路由
- nuxt4的文件结构
- stores 、composables差不多

# 了解一个新东西对于一些人来说很痛苦
删删减减，了解里面的逻辑吧

来几个任务

1. 请问为什么，不能直接访问app入口文件里面的页面。 
app是入口文件。我一直以为只要在入口文件新加html文件就可以在浏览器中直接访问。这个功能放到了pablic文件夹里面。
2. 路由逻辑是怎样子的，有几大要素。
3. 状态管理有什么作用，是如何使用的。
4. 一个vue包含了那些标签
5. 使用stores中的counter.ts，pinia模块实现一个类似的加减工具。
6. 等等等


# 后期有时间试一试分支功能，每个分支就只关注一个功能或者一类功能，比如路由，状态管理等等



# Nuxt Minimal Starter
以下是没有什么用处的自动生成的，不会告诉你如何修改端口好的

Look at the [Nuxt documentation](https://nuxt.com/docs/getting-started/introduction) to learn more.

## Setup

Make sure to install dependencies:

```bash
# npm
npm install

# pnpm
pnpm install

# yarn
yarn install

# bun
bun install
```

## Development Server

Start the development server on `http://localhost:3000`:

```bash
# npm
npm run dev

# pnpm
pnpm dev

# yarn
yarn dev

# bun
bun run dev
```

## Production

Build the application for production:

```bash
# npm
npm run build

# pnpm
pnpm build

# yarn
yarn build

# bun
bun run build
```

Locally preview production build:

```bash
# npm
npm run preview

# pnpm
pnpm preview

# yarn
yarn preview

# bun
bun run preview
```

Check out the [deployment documentation](https://nuxt.com/docs/getting-started/deployment) for more information.
